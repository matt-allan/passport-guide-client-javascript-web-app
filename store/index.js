export const state = () => {
  return {
    auth: null
  }
}
export const mutations = {
  // prettier-ignore
  setAuth(state, auth) {
    state.auth = auth
  }
}

// prettier-ignore
export const actions = {
}
